# mlir-sbt

这是一个基于 MLIR 的二进制翻译框架，
我希望基于一种新的中间表示，因为 LLVM IR 或许缺少一些翻译辅助的描述指令。
或许翻译框架与编译框架有不同之处，翻译框架可以有一些辅助的指令，这些指令与整体的架构有关。
其他情况下，指令直接借鉴原有 LLVM IR 即可。

对于中间表示，应当尽可能简洁，只有基础的操作，后面再通过指令选择特化为架构的一些高级指令。

## Step1 

完成 x86 -> x86 的指令翻译，尝试搭建一个 IR, 并且降级为LLVM IR，最后生成代码

## 文档

本项目的知识文档见 docs 目录

## 代码编写

先查看代码，是否有可以利用的函数